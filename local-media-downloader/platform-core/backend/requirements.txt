fastapi==0.141.1
starlette==1.3.1
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2
python-multipart==0.0.32
sqlalchemy==2.0.35
alembic==1.13.3
# PostgreSQL driver (mission 4, phase 2) — psycopg3, binary wheel so no
# build toolchain is needed in the runtime image. SQLite (the default)
# needs no driver; this is only imported when DATABASE_URL uses
# postgresql+psycopg://.
psycopg[binary]>=3.2,<4
argon2-cffi==23.1.0
pyjwt==2.13.0
cryptography>=42,<46
email-validator==2.2.0
jinja2==3.1.4
httpx==0.27.2
pytest==8.4.2
pytest-asyncio==0.24.0
